#include<iostream>
#include<vector>
using namespace std;
int main(){
  vector<vector<int>> vec(3, vector<int> (3));
  //it initialises a vector of vectors of size row=3 and col=3
  //but all the indeces are currently at 0
  vec[0][0] = 1;
  cout<<vec[0][0];

  v.clear(); to clear all the elements;
}