# C++ String `substr` Method

The `substr` method is a part of the `std::string` class in C++. It is used to extract a part of a string.

## Syntax

```cpp
string substr(size_t pos = 0, size_t len = npos) const;
```
